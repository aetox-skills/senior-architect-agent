flowchart LR
  %% Observed from provided file tree and signals.
  %% Future Backend API and Future Database are not observed.
  %% Dotted edges are open questions, not confirmed architecture.

  User[User] --> App

  subgraph BrowserBoundary[Observed Browser Boundary]
    App[React App]
    TodoList[TodoList Component]
    TodoStorage[todoStorage Service]
    BrowserStorage[(Browser Storage)]

    App --> TodoList
    TodoList --> TodoStorage
    TodoStorage --> BrowserStorage
  end

  BackendAPI[Future Backend API]:::future
  Database[(Future Database)]:::future

  TodoStorage -.-> BackendAPI
  BackendAPI -.-> Database

  classDef future stroke-dasharray: 5 5;
