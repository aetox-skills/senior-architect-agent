flowchart LR
  %% Observed from provided file tree and signals.
  User[User] --> App

  subgraph Browser["Observed browser boundary"]
    App[React App]
    TodoList[TodoList Component]
    TodoStorage[todoStorage Service]
    BrowserStorage[(Browser Storage)]

    App --> TodoList
    TodoList --> TodoStorage
    TodoStorage --> BrowserStorage
  end

  %% Not observed. Included only because the user request is before adding a backend.
  BackendAPI[Future Backend API<br/>not observed]:::future
  Database[(Future Database<br/>not observed)]:::future

  TodoStorage -. "open question: backend persistence" .-> BackendAPI
  BackendAPI -. "open question: database ownership" .-> Database

  classDef future stroke-dasharray: 5 5;
